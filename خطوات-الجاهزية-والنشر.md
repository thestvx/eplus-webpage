# خطوات الجاهزية والنشر اليدوية — E-PLUS (بالترتيب)

> لا أي commit/push قبل تنفيذك لهذه الخطوات وإرسال النتائج.
> القيم المرجعية: مشروع Supabase `jftfvpultaqufhsekdle` — Firebase Project `eplus-center-39`.

---

## ⚠️ الإصلاحات الأمنية الجديدة (نُفِّذت في الكود — تحتاج نشراً فقط)

| الملف | ما تغيّر |
|---|---|
| `functions/index.js` | حُذف **مفتاح Resend المضمَّن** (`re_Jo35...`). يُقرأ الآن من `RESEND_API_KEY` (متغير بيئة) أو `firebase functions:config:set resend.apikey`. أُضيف حارس: بدون مفتاح → 500 برسالة واضحة. وقُيّد CORS (بدل `*`). |
| `email-worker/src/index.ts` | CORS = allowlist بدل `*` (بلا إجابة أصل مرفوض). المفتاح يبقى من `env.RESEND_API_KEY`. |
| `email-worker.js` (جذر) | نفس إصلاح CORS (نسخة مكررة — يُفضَّل حذفها لاحقاً). |
| `supabase-migration.sql` | **خطأ حقيقي أُصلح**: توقيع `admin_create_subscription` في `REVOKE` (سطر 848) و`GRANT` (سطر 875) كان قديماً بـ 7 معاملات بينما الدالة الآن 11 — كان سيوقف التهجير. صُحِّح إلى 11. تحقّقت آلياً: 45 مرجع GRANT/REVOKE متطابق مع التعريفات (0 خطأ). |

**إجراء عاجل بيدك**: المفتاح القديم `re_Jo35...` أصبح **مكشوفاً في git** → أبطئه الآن من لوحة Resend (Settings → API Keys → Revoke) وولّد مفتاحاً جديداً ستستخدمه في الخطوات أدناه.

---

## الخطوة 1 — تشغيل `supabase-migration.sql` (مرة واحدة)

**أين:** dashboard.supabase.com → مشروع `jftfvpultaqufhsekdle` → **SQL Editor** → New query

1. افتح `supabase-migration.sql` من جهازك، انسخ **المحتوى كاملاً**، الصقه، واضغط **Run**.
2. **هل أشغّله كاملاً مرة واحدة؟** نعم. كل الأوامر `IF NOT EXISTS` / `CREATE OR REPLACE` — لا تحذف بيانات، لكن شغّله مرة واحدة فقط.
3. **النتيجة المتوقعة:** كل statement يعرض `Success`؛ لا أي رسالة error.
   - إن ظهر error → انسخه كاملاً وأرسله لي (لا تكمّل الخطوات التالية).

**بعد النجاح — تحقق إنشاء كل شيء (شغّل الحذفية التالية وأرسل لي ناتجها):**

```sql
-- 1) أعمدة الاشتراك (متوقع: teacher_id, subject_id, teacher_name, subject_name ظاهرة)
SELECT column_name FROM information_schema.columns
WHERE table_name='student_subscriptions' ORDER BY ordinal_position;

-- 2) الفهارس (متوقع: idx_subscriptions_subject_teacher + idx_subscriptions_payment)
SELECT indexname FROM pg_indexes WHERE tablename='student_subscriptions';

-- 3) كل دوال admin_* (متوقع 19 دالة + args صحيحة)
SELECT p.proname, pg_get_function_arguments(p.oid) AS args
FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public' AND p.proname LIKE 'admin_%' ORDER BY p.proname;

-- 4) الدوال العامة (متوقع: is_admin, get_student_subscription, get_student_attendance_events, record_attendance, undo_attendance)
SELECT p.proname FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public' AND p.proname IN
('is_admin','get_student_subscription','get_student_attendance_events','record_attendance','undo_attendance');

-- 5) RLS مفعّل على الجداول السبعة (متوقع 7 صفوف)
SELECT tablename FROM pg_tables
WHERE schemaname='public' AND tablename IN
('teacher_balances','teacher_transactions','teacher_receipts',
 'student_subscriptions','subscription_periods','attendance_sessions','admin_users')
AND rowsecurity ORDER BY tablename;
```

- **تنظيف اختياري** لو سبق أن شغّلت نسخة قديمة من الـ SQL (إزالة الدالة القديمة 7 معاملات إن وُجدت):
  `DROP FUNCTION IF EXISTS admin_create_subscription(TEXT, TEXT, TEXT, INT, INT, TEXT, TEXT);`

**إدراج حساب المدير (حاسم — بدونه admin-api يرجع 403):**
1. اجلب UID الخاص بحسابك: **Firebase Console** → Authentication → Users → انسخ UID (العمود User UID).
2. نفّذ:
```sql
INSERT INTO admin_users (uid, role, display_name)
VALUES ('<UID-الخاص-بك>', 'superadmin', 'المدير');
SELECT uid, role FROM admin_users;
```

**ماذا ترسل لي:** ناتج الحذفيات الخمس + الـ UID المستخدم.

---

## الخطوة 2 — نشر Edge Functions الثلاثة

النظام يعتمد فعلياً على 3 دوال فقط (لا غيرها): `admin-api` (لوحة الأدمن والمالية والاشتراكات)، `record-attendance` (الحضور)، `content-upload` (رفع ملفات الأستاذ).

**بالـ CLI (موصى به — يضم مجلد `_shared/` تلقائياً):**
```powershell
supabase login
supabase link --project-ref jftfvpultaqufhsekdle
supabase functions deploy record-attendance --no-verify-jwt
supabase functions deploy admin-api --no-verify-jwt
supabase functions deploy content-upload --no-verify-jwt
```
`--no-verify-jwt` ضروري: التوكن من Firebase وليس Supabase (مؤكد في الـ migration سطر 896).

**تحقق CORS فوراً (بدون متصفح):**
```powershell
# نطاق مسموح → متوقع: HTTP/2 200 + access-control-allow-origin: https://epluscenter.com + vary: origin
curl.exe -i -X OPTIONS "https://jftfvpultaqufhsekdle.supabase.co/functions/v1/admin-api" -H "Origin: https://epluscenter.com" -H "Access-Control-Request-Method: POST"
# نطاق مرفوض → متوقع: HTTP/2 200 وبدون أي access-control-allow-origin إطلاقاً
curl.exe -i -X OPTIONS "https://jftfvpultaqufhsekdle.supabase.co/functions/v1/admin-api" -H "Origin: https://evil.example" -H "Access-Control-Request-Method: POST"
```
كرر الاثنين مع `record-attendance` و `content-upload` أيضاً.

**أداة النشر البديلة (إن تعذّر CLI):** Dashboard → Edge Functions → الدالة → Deploy → لصق الملف. **تحذير:** بهذه الطريقة لا يُرفع مجلد `_shared/` تلقائياً فتفشل الاستيرادات (`../_shared/cors.ts`). إن اضطررت لذلك أرسل لي وأنتظر بديلاً.

**ماذا ترسل لي:** ناتج أوامر الـ CLI + مخرجات الـ curl الستة (3 دوال × 2 نطاق).

---

## الخطوة 3 — البريد الإلكتروني (اختياري، إن استُخدم)

الواجهة الحالية ترسل عبر **Cloudflare Worker** (`NOTIF_PROXY_URL` في `admin.html` سطر 3948) وليس عبر دالة Firebase.

**أ) Cloudflare Worker:**
```powershell
cd email-worker
npx wrangler login
npx wrangler deploy
npx wrangler secret put RESEND_API_KEY   # الصق المفتاح الجديد من Resend
```
ثم في `admin.html` سطر 3948 استبدل
`'https://email-worker.your-subdomain.workers.dev'` بالرابط الفعلي (Cloudflare Dashboard → Workers & Pages → email-worker → احفظ الرابط الذي يبدأ بـ `https://email-worker.<...>.workers.dev`).

**ب) دالة Firebase `functions/index.js` (اختيارية — لا تستدعيها الواجهة حالياً):**
```powershell
cd functions
firebase login
firebase functions:config:set resend.apikey="<المفتاح-الجديد>"
firebase deploy --only functions --project eplus-center-39
```
ملاحظة: `firebase.json` لا يحتوي كتلة `functions` ولا يوجد `.firebaserc` — إن فشل الأمر أرسل لي النص.

**ماذا ترسل لي:** نتيجة إرسال بريد تجريبي من قسم «الإشعارات» في لوحة الأدمن (نجاح/فشل + أي خطأ).

---

## الخطوة 3.5 — نشر الواجهات المحدّثة (admin.html / attendance.html)

الموقع مستضاف على **Firebase Hosting** (`firebase.json` → `public: "."`). بدون هذه الخطوة ستختبر نسخاً قديمة:
```powershell
firebase login
firebase deploy --only hosting --project eplus-center-39
```
**النتيجة:** يعرض رابط النشر + قائمة الملفات المرفوعة (تأكد أن `admin.html` و `attendance.html` و `js/subscriptionService.js` في القائمة).

---

## الخطوة 4 — اختبار Production: الاشتراكات (تسلسل طلبك)

افتح `https://epluscenter.com` (أو النطاق الذي يعمل منه) وسجّل دخول كأدمن.

| ماذا تفعل | النتيجة المتوقعة |
|---|---|
| 1. تسجيل الدخول كأدمن | لوحة الأدمن تفتح |
| 2. افتح «الاشتراكات الشهرية» | القائمة تفتح (قد تكون فارغة) بدون «تعذر تحميل البيانات» |
| 3. ابحث عن طالب بالاسم واللقب | قائمة منسدلة تظهر الاسم الكامل |
| 4. ابحث بالـ Student ID | يظهر الطالب المقصود بالضبط |
| 5. انقر حقل المسح ثم مرّر باركود SP-1160 | بطاقة الطالب تظهر فوراً (المادة + الأستاذ) |
| 6. اختر المادة ثم الأستاذ | الزر يتفعّل بعد اختيارهما فقط |
| 7. أنشئ اشتراك شهر واحد | رسالة نجاح + يظهر في القائمة بالمادة والأستاذ |
| 8. افتح التفاصيل | شهر واحد: total=8, used=0, remaining=8 |

**اختبار شهرين و3 أشهر بدون حفظ بيانات تجريبية** — في SQL Editor استعمل معاملة تُتراجع (مادة/طالب حقيقيين، تاريخ مستقبلي لتجنّب التداخل):
```sql
BEGIN;
SELECT jsonb_pretty(admin_create_subscription('<UID-المدير>', '<student-id>', '2026-09-01', 3, 6000,
  'TEST-M3-<' + 'random>', NULL, '<teacher-id>', '<subject-id>', '<teacher-name>', '<subject-name>'));
ROLLBACK;   -- التراجع يمسح كل شيء — لا يُحفظ
```
- متوقع لـ 3 أشهر: `periods` = 3 و `total_sessions` = 24. (كرر للشهرين → 16.)
- اجلب student-id / teacher-id / subject-id من قاعدة البيانات (قائمة الطلاب في الواجهة، أو `SELECT id FROM registrations;`).

**ماذا ترسل لي:** لقطات الخطوات 1–8 + ناتج SQL للشهرين والثلاثة.

---

## الخطوة 5 — اختبار Finance فعلياً (كل شيء يمر عبر admin-api)

لوحة الأدمن → «المالية»:
1. **listBalances** → قائمة الأرصدة تظهر.
2. **getBalance** → عند اختيار أستاذ يظهر رصيده.
3. **getLedger** → سجل المعاملات (قد يكون فارغاً).
4. **getReceipts** → الإيصالات (قد تكون فارغة).
5. **addPayment** → أضف دفعة صغيرة حقيقية لأستاذ → يظهر الإيصال ويُحدَّث الرصيد.
6. **recomputeBalance** → زر إعادة الاحتساب → تظهر الحصص المحسوبة من الحضور السابق إن وُجد.

**إذا ظهر `Failed to fetch` أو خطأ CORS** → افتح DevTools (F12) → Network → انسخ لي حالة الطلب + رؤوس الاستجابة. يدوياً:
```js
fetch('https://jftfvpultaqufhsekdle.supabase.co/functions/v1/admin-api',
  { method:'OPTIONS', headers:{ Origin:'https://epluscenter.com', 'Access-Control-Request-Method':'POST' }})
  .then(r=>console.log('status', r.status, 'allow-origin', r.headers.get('Access-Control-Allow-Origin')));
```
متوقع: `status 200` و `allow-origin https://epluscenter.com`.

**ماذا ترسل لي:** لقطة لكل شاشة + ناتج الكنزول/Network (إن وُجد خطأ).

---

## الخطوة 6 — اختبار Attendance فعلياً (SP-1160 حقيقي)

Printed Student Card → Smart SP-1160 → `attendance.html`:
1. اضغط زر المسح (أو انقر حقل الباركود) في `attendance.html`.
2. مرّر الباركود → الطالب يظهر.
3. اختر المادة → يظهر الأستاذ/الاشتراك المناسب.
4. سجّل الحضور → رسالة نجاح + **خصم حصة واحدة من شهر المادة الصحيح فقط**.

**التحقق من قاعدة البيانات بعد الحضور:**
```sql
SELECT s.id, s.subject_id, s.teacher_id, p.month_number, p.used_sessions, p.remaining_sessions
FROM subscription_periods p JOIN student_subscriptions s ON s.id=p.subscription_id
WHERE s.student_id='<student-id>' ORDER BY s.created_at DESC;

SELECT id, subject_id, teacher_id, status, date FROM attendance_sessions ORDER BY created_at DESC LIMIT 5;
```

**ماذا ترسل لي:** لقطة الواجهة + ناتج الاستعلامين.

---

## الخطوة 7 — اختبار العزل: الرياضيات+أستاذ أ ≠ الفيزياء+أستاذ ب

لطالب له اشتراكان (رياضيات/أ + فيزياء/ب):
1. سجّل حضوراً في الرياضيات → تحقق: `used` زاد في شهر **الرياضيات فقط** (الاستعلام أعلاه) والفيزياء لم تتغيّر.
2. سجّل حضوراً في الفيزياء → يخصم من **الفيزياء** وليس الرياضيات.
3. **حالة سلبية:** طالب له اشتراك رياضيات فقط، جرّب حضور فيزياء → متوقع: «لا يوجد اشتراك نشط لهذه المادة عند هذا الأستاذ.» وبدون أي خصم.

**ماذا ترسل لي:** ناتج الاستعلام بعد كل حضور + لقطة الحالة السلبية.

---

## الخطوة 8 — تحديد نطاق www

1. في المتصفح على الموقع: F12 → Console → شغّل `location.origin` → اكتب لي النتيجة.
   (أو Network tab → أي طلب → Request Headers → قيمة `origin`.)
2. تأكد من قبول CORS لذلك النطاق:
```powershell
curl.exe -i -X OPTIONS "https://jftfvpultaqufhsekdle.supabase.co/functions/v1/admin-api" -H "Origin: <ما-ظهر-في-location.origin>" -H "Access-Control-Request-Method: POST"
```
متوقع: `access-control-allow-origin: <نفس النطاق>`.
- كلا `https://epluscenter.com` و `https://www.epluscenter.com` موجودان مسبقاً في كل قوائم الـ allowlist (supabase + email-worker + functions/index.js)، فأي النطاقين يعمل مقبول.
- إذا ظهر نطاق **ثالث** غريب (مثل `...firebaseapp.com`) → أرسله لي ولا تكمّل.

**ماذا ترسل لي:** قيمة `location.origin` + ناتج الـ curl.

---

## ملخص: ماذا تعمل يدوياً / أين / النتيجة / ماذا ترسل

| # | ماذا | أين | النتيجة المتوقعة | ترسل لي |
|---|---|---|---|---|
| 1 | تشغيل الـ migration كاملاً | Supabase SQL Editor | Success بلا أخطاء | ناتج حذفيات التحقق الخمس + UID |
| 1ب | إدراج حساب المدير | Supabase SQL Editor | صف admin_users | `SELECT uid, role FROM admin_users` |
| 2 | نشر 3 Edge Functions | `supabase functions deploy ...` | Success لكل دالة | ناتج CLI + curl الستة |
| 3 | (اختياري) البريد + تحديث NOTIF_PROXY_URL | wrangler + admin.html | إرسال تجريبي ناجح | نتيجة تجربة الإشعارات |
| 3.5 | نشر الواجهات | `firebase deploy --only hosting` | admin.html/attendance.html مرفوعة | قائمة الملفات المرفوعة |
| 4 | اختبار الاشتراكات (SP-1160 + 1/2/3 أشهر) | الموقع + SQL Editor | 8/16/24 حصة + بطاقة تظهر بالمسح | لقطات + ناتج SQL |
| 5 | Finance فعلي | لوحة الأدمن → المالية | أرصدة/دفتر/إيصالات/دفعة تعمل | لقطات + أي خطأ Console |
| 6 | Attendance فعلي (SP-1160) | attendance.html | حضور ناجح + خصم حصة | لقطة + استعلامي التحقق |
| 7 | عزل مادة/أستاذ | attendance.html + SQL | خصم من المادة الصحيحة فقط | ناتج الاستعلام + لقطة |
| 8 | تحديد نطاق www + CORS | Console + curl | ACAO = نطاق الموقع | `location.origin` + ناتج curl |

---

## الفصل الصريح بين الاختبارات (لم نعدّ أي «تم» قبلها)

- **A) اختبارات الكود (مكتملة، لا تكفي وحدها):** 312 فحصاً + TZ + CORS worker 9/9 + TS 4/4 + تطابق 45 GRANT/REVOKE — كلها خضراء.
- **B) قاعدة البيانات الحقيقية:** الخطوة 1 (SQL + التحقق + إدراج المدير) واختبار الشهرين/الثلاثة بـ BEGIN/ROLLBACK.
- **C) Production الحقيقي:** الخطوات 2–8 بعد النشر (بما فيها Finance و Attendance و www).
- **D) SP-1160 الفعلي:** الخطوة 4.5 (مسح في الاشتراكات) والخطوة 6 (مسح في الحضور) — بجهازك الحقيقي.

**لا commit/push حتى تستلم نتائجي لك من الخطوات أعلاه.**
